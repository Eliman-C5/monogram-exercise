import React from 'react'

export const TransparentImage = ({positions}) => {
  return (
    <div className={`max-w-[280px] w-full block max-h-[479px] h-full ${positions} gradient-image`}>
    </div>
  )
}
